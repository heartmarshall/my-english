🛠 Технологический стек
Драйвер: pgx/v5 — высокопроизводительный драйвер и пулер соединений.
Query Builder: squirrel — построение SQL-запросов через Fluent API.
Scanning: scany/v2 — удобный маппинг результатов SQL в Go-структуры.

Все имена таблиц и колонок определены как константы.
Методы .Eq(), .Lt(), .In() и др. предотвращают ошибки в названиях полей на этапе компиляции.
Поддержка алиасов (в планах/реализована частично) для безопасных JOIN-ов.

Структура
```
internal/database/
├── database.go       # Базовые хелперы (Select, GetOne, Exec) и инициализация
├── errors.go         # Кастомные ошибки (ErrNotFound, ErrDuplicate)
├── tx.go             # TxManager для управления транзакциями
├── schema/           # Описание таблиц и колонок (Type-Safe)
├── word/             # Репозиторий слов
├── meaning/          # Репозиторий значений (вкл. SRS логику)
├── example/          # Репозиторий примеров
├── tag/              # Репозиторий тегов
└── meaningtag/       # Репозиторий связей (M2M)
```

### 📦 Word (Слова)
*   **Изменение:** `Create`, `Update`, `Delete`
*   **Поиск:** `GetByID`, `GetByText`
*   **Списки:** `List` (с фильтром и пагинацией), `Count`, `Exists`

### 🧠 Meaning (Значения и SRS)
*   **База:** `Create`, `Update`, `Delete`, `DeleteByWordID`
*   **Поиск:** `GetByID`, `GetByWordID`, `List`, `Count`
*   **Обучение (SRS):**
    *   `GetStudyQueue` — очередь на изучение (новые + повторение)
    *   `GetDueForReview` — только на повторение
    *   `UpdateSRS` — обновить статус/интервал после ответа
    *   `GetStats` — статистика (сколько выучено/в процессе)

### 📝 Example (Примеры предложений)
*   **Изменение:** `Create`, `CreateBatch` (сразу пачкой), `Update`, `Delete`
*   **Поиск:** `GetByID`, `GetByMeaningID`

### 🏷️ Tag (Теги)
*   **Изменение:** `Create`, `Delete`
*   **Умное создание:** `GetOrCreate` (найдет старый или создаст новый)
*   **Поиск:** `GetByID`, `GetByName`, `List`

### 🔗 MeaningTag (Связь Значение-Тег)
*   **Связывание:** `AttachTag`, `DetachTag`, `SyncTags` (полная синхронизация)
*   **Поиск:** Получить теги для значения или значения для тега.