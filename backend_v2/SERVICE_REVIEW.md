# Ревью сервисного слоя приложения

## Обзор

Проведен анализ сервисного слоя приложения для изучения английского языка. Ниже представлены найденные проблемы и недостающий функционал.

---

## ✅ Что реализовано хорошо

1. **Card Service** - базовый CRUD функционал
   - ✅ Create - создание карточек с тегами и SRS состоянием
   - ✅ GetByID - получение карточки
   - ✅ Delete - мягкое удаление
   - ✅ Базовая структура для Update

2. **Dictionary Service** - работа со словарем
   - ✅ Search - поиск с кэшированием и внешними провайдерами
   - ✅ GetLexeme - получение лексемы
   - ✅ SaveImportedWord - сохранение импортированных слов

3. **Study Service** - изучение карточек
   - ✅ Review - обработка ответов пользователя с SRS алгоритмом
   - ✅ GetQueue - получение очереди для изучения

4. **Inbox Service** - входящие элементы
   - ✅ Полный CRUD функционал

5. **Stats Service** - статистика
   - ✅ GetDashboardStats - статистика дашборда

---

## ❌ Критические проблемы

### 1. Card Service: Update не сохраняет изменения в БД

**Файл:** `internal/service/card/update.go:40-44`

**Проблема:** Метод `Update` получает карточку, обновляет поля в памяти, но не вызывает `cardRepo.Update()` для сохранения в БД.

```go
// TODO: Добавить метод Update в CardRepository.
// Для MVP пока опустим детали реализации Update в репо, но логика сервиса такая:
// _, err = cardRepo.Update(ctx, current)

updatedCard = current
```

**Решение:** Метод `Update` уже существует в репозитории (`internal/database/repository/card/repository.go:77`), нужно его вызвать.

---

### 2. Card Service: Фильтрация в List не реализована

**Файл:** `internal/service/card/service.go:45-54`

**Проблема:** Метод `List` игнорирует параметр `filter`. В GraphQL схеме ожидаются:
- `search: String` - поиск по тексту
- `tags: [String!]` - фильтр по тегам
- `statuses: [LearningStatus!]` - фильтр по статусам

**Текущая реализация:**
```go
// TODO: Реализовать фильтрацию по тегам (потребует JOIN или subquery в репозитории)
// Пока возвращаем просто список активных
```

**Решение:** Нужно реализовать:
1. Фильтрацию по тегам (JOIN с `card_tags` и `tags`)
2. Фильтрацию по статусам (JOIN с `srs_states`)
3. Поиск по тексту (по `custom_text` или через `sense -> lexeme -> text`)

---

### 3. Study Service: Фильтрация в GetQueue не реализована

**Файл:** `internal/service/study/queue.go:11`

**Проблема:** Метод `GetQueue` не принимает параметр фильтра, хотя в GraphQL схеме `StudyFilter` поддерживает:
- `tags: [String!]`
- `statuses: [LearningStatus!]`
- `limit: Int`

**Текущая реализация:**
```go
func (s *Service) GetQueue(ctx context.Context, limit int) ([]model.Card, error)
```

**Решение:** Нужно:
1. Добавить параметр `filter *StudyFilter` в метод
2. Реализовать фильтрацию по тегам (JOIN)
3. Реализовать фильтрацию по статусам (уже есть в `ListDueForReview`, но нужно расширить)

---

### 4. Card Service: Update не обрабатывает очистку тегов

**Файл:** `internal/service/card/update.go:51-87`

**Проблема:** Логика обновления тегов срабатывает только если `len(input.Tags) > 0`. Если пользователь хочет удалить все теги (передает пустой массив), они не удалятся.

**Решение:** Изменить условие:
```go
// Вместо:
if len(input.Tags) > 0 {
    // ...
}

// Должно быть:
if input.Tags != nil {  // nil = не обновлять, [] = очистить
    // ...
}
```

---

## ⚠️ Важные улучшения

### 5. Card Service: Filter структура неполная

**Файл:** `internal/service/card/dto.go:30-33`

**Проблема:** Структура `Filter` содержит только `Tags`, но GraphQL схема ожидает также:
- `Search: *string`
- `Statuses: []model.LearningStatus`

**Решение:** Расширить структуру:
```go
type Filter struct {
    Search   *string
    Tags     []string
    Statuses []model.LearningStatus
}
```

---

### 6. Study Service: Нет структуры Filter

**Файл:** `internal/service/study/queue.go`

**Проблема:** Нет DTO для фильтрации очереди изучения.

**Решение:** Создать файл `internal/service/study/dto.go`:
```go
type Filter struct {
    Tags     []string
    Statuses []model.LearningStatus
}
```

---

### 7. Card Service: Update не обновляет CustomText

**Файл:** `internal/service/card/update.go:29-33`

**Проблема:** Метод `Update` не обновляет поле `CustomText`, хотя в GraphQL схеме это поле может быть изменено (через `customText` в `UpdateCardInput`).

**Примечание:** Проверить GraphQL схему - возможно, `CustomText` не должен изменяться после создания (это логично, так как это базовое поле карточки).

---

### 8. Dictionary Service: Нет обработки ошибки "lexeme not found"

**Файл:** `internal/service/dictionary/service.go:34-36`

**Проблема:** Метод `GetLexeme` не возвращает специфичную ошибку `ErrLexemeNotFound`, хотя она определена в `service/errors.go:27`.

**Решение:** Добавить проверку:
```go
lexeme, err := s.repos.Lexeme(s.txManager.Q()).GetByID(ctx, "id", id)
if err != nil {
    if database.IsNotFoundError(err) {
        return nil, service.ErrLexemeNotFound
    }
    return nil, err
}
```

---

## 📋 Дополнительные замечания

### 9. Card Service: Сортировка в List

**Проблема:** Метод `List` не поддерживает сортировку. Обычно карточки сортируют по:
- `created_at DESC` (новые сначала)
- Или по `updated_at DESC`

**Решение:** Добавить параметр сортировки или использовать дефолтную сортировку.

---

### 10. Study Service: GetQueue не учитывает фильтр из resolver

**Файл:** `graph/schema.resolvers.go:232-247`

**Проблема:** Resolver получает `filter *StudyFilter`, но передает в сервис только `limit`, игнорируя `tags` и `statuses`.

**Решение:** Обновить resolver для передачи фильтра в сервис.

---

### 11. Card Service: Валидация в Create

**Файл:** `internal/service/card/create.go:15-17`

**Замечание:** Валидация проверяет, что либо `SenseID`, либо `CustomText` задан. Но не проверяет, что `CustomText` не пустой (есть `strings.TrimSpace`, но проверка происходит только если `CustomText == nil`).

**Улучшение:** Добавить проверку на пустую строку после trim.

---

## 🎯 Приоритеты реализации

### Высокий приоритет (блокирует работу)
1. ✅ **Card Service: Update не сохраняет в БД** - критично, карточки не обновляются
2. ✅ **Card Service: Фильтрация в List** - обязательный функционал по схеме
3. ✅ **Study Service: Фильтрация в GetQueue** - обязательный функционал по схеме

### Средний приоритет (важно для UX)
4. ✅ **Card Service: Очистка тегов в Update**
5. ✅ **Card Service: Расширение Filter структуры**
6. ✅ **Study Service: Создание Filter DTO**

### Низкий приоритет (улучшения)
7. ✅ **Dictionary Service: Обработка ErrLexemeNotFound**
8. ✅ **Card Service: Сортировка в List**
9. ✅ **Card Service: Улучшение валидации**

---

## 📝 Рекомендации по архитектуре

1. **Единообразие DTO**: Все сервисы должны иметь DTO в отдельных файлах (`dto.go`)
2. **Валидация**: Рассмотреть использование библиотеки валидации (например, `validator`)
3. **Тестирование**: Добавить unit-тесты для критичных методов (особенно Update и фильтрация)
4. **Документация**: Добавить godoc комментарии к публичным методам

---

## Итоговая оценка

**Общее состояние:** 🟡 Среднее

**Готовность к продакшену:** ~70%

**Основные блокеры:**
- Update карточек не работает
- Фильтрация не реализована
- Часть функционала из GraphQL схемы не поддерживается

**Рекомендация:** Исправить критические проблемы перед деплоем.

