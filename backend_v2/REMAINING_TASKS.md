# Оставшиеся задачи для улучшения сервисного слоя

## ✅ Выполненные задачи (высокий приоритет)

1. ✅ Card Service: Update - сохранение в БД
2. ✅ Card Service: Фильтрация в List (tags, statuses, search)
3. ✅ Study Service: Фильтрация в GetQueue (tags, statuses)
4. ✅ Card Service: Очистка тегов в Update
5. ✅ Card Service: Расширение Filter структуры
6. ✅ Study Service: Создание Filter DTO
7. ✅ Card Service: Сортировка в List
8. ✅ Resolvers: Обновление для передачи фильтров

---

## 🔧 Оставшиеся задачи

### Средний приоритет

#### 1. ✅ Dictionary Service: Обработка ErrLexemeNotFound - ВЫПОЛНЕНО

**Файл:** `internal/service/dictionary/service.go:34-36`

**Проблема:** Метод `GetLexeme` не возвращает специфичную ошибку `ErrLexemeNotFound`, хотя она определена и используется в resolver.

**Текущий код:**
```go
func (s *Service) GetLexeme(ctx context.Context, id uuid.UUID) (*model.Lexeme, error) {
	return s.repos.Lexeme(s.txManager.Q()).GetByID(ctx, "id", id)
}
```

**Решение:** Добавить обработку ошибки:
```go
func (s *Service) GetLexeme(ctx context.Context, id uuid.UUID) (*model.Lexeme, error) {
	lexeme, err := s.repos.Lexeme(s.txManager.Q()).GetByID(ctx, "id", id)
	if err != nil {
		if database.IsNotFoundError(err) {
			return nil, service.ErrLexemeNotFound
		}
		return nil, err
	}
	return lexeme, nil
}
```

**Важность:** Улучшает обработку ошибок и согласованность с остальным кодом.

---

#### 2. ✅ Card Service: Валидация SenseID при создании - ВЫПОЛНЕНО

**Файл:** `internal/service/card/create.go:15-17`

**Проблема:** При создании карточки с `SenseID` не проверяется, существует ли этот Sense в БД.

**Текущий код:**
```go
if input.SenseID == nil && (input.CustomText == nil || strings.TrimSpace(*input.CustomText) == "") {
	return nil, service.ErrInvalidInput
}
```

**Решение:** Добавить проверку существования Sense:
```go
// Если указан SenseID, проверяем его существование
if input.SenseID != nil {
	sense, err := s.repos.Sense(s.txManager.Q()).GetByID(ctx, *input.SenseID)
	if err != nil {
		if database.IsNotFoundError(err) {
			return nil, service.ErrSenseNotFound
		}
		return nil, err
	}
	if sense == nil {
		return nil, service.ErrSenseNotFound
	}
}
```

**Важность:** Предотвращает создание карточек с несуществующими Sense.

---

#### 3. Card Service: Валидация CustomText (улучшение)

**Файл:** `internal/service/card/create.go:15-17`

**Текущая реализация:** Проверка есть, но можно улучшить:
```go
if input.SenseID == nil && (input.CustomText == nil || strings.TrimSpace(*input.CustomText) == "") {
	return nil, service.ErrInvalidInput
}
```

**Улучшение:** Нормализовать CustomText сразу при валидации:
```go
// Нормализуем CustomText если он указан
if input.CustomText != nil {
	trimmed := strings.TrimSpace(*input.CustomText)
	if trimmed == "" {
		return nil, service.ErrInvalidInput
	}
	input.CustomText = &trimmed
}
```

**Важность:** Низкая, текущая реализация работает, но улучшение сделает код чище.

---

### Низкий приоритет (улучшения качества кода)

#### 4. Добавление godoc комментариев

**Проблема:** Не все публичные методы имеют документацию.

**Решение:** Добавить godoc комментарии ко всем публичным методам сервисов.

**Пример:**
```go
// Create создает новую карточку для изучения.
// Если указан SenseID, карточка связывается с существующим смыслом из словаря.
// Если указан CustomText, создается полностью кастомная карточка.
// Обязательно должен быть указан либо SenseID, либо CustomText.
func (s *Service) Create(ctx context.Context, input CreateCardInput) (*model.Card, error)
```

---

#### 5. Улучшение обработки ошибок в Dictionary Service

**Файл:** `internal/service/dictionary/search.go`

**Проблема:** При ошибках провайдеров логируется предупреждение, но нет общего механизма обработки критических ошибок.

**Улучшение:** Добавить метрики/статистику успешных/неуспешных запросов к провайдерам.

---

#### 6. Валидация лимитов в фильтрах

**Проблема:** В методах `List` и `GetQueue` не валидируются лимиты (могут быть отрицательными или слишком большими).

**Текущее состояние:** Валидация есть в `repository.WithPagination`, но можно добавить явную валидацию на уровне сервиса.

**Решение:** Добавить проверку лимитов перед вызовом репозитория:
```go
if limit <= 0 {
	limit = repository.DefaultLimit
}
if limit > repository.MaxLimit {
	limit = repository.MaxLimit
}
```

---

#### 7. Проверка существования SenseID в Update (если будет добавлено)

**Примечание:** В текущей GraphQL схеме `UpdateCardInput` не содержит `senseId`, так что это не актуально. Но если в будущем добавится возможность изменения SenseID, нужно будет добавить валидацию.

---

## 🚀 Дополнительные улучшения для продакшена

### 8. Добавление unit-тестов

**Критичные методы для тестирования:**
- `CardService.Create` - создание карточек с тегами
- `CardService.Update` - обновление и синхронизация тегов
- `CardService.List` - фильтрация по тегам, статусам, поиску
- `StudyService.GetQueue` - фильтрация очереди
- `StudyService.Review` - обработка ответов и SRS алгоритм

**Рекомендация:** Использовать `testify` для тестов и моки.

---

### 9. Добавление интеграционных тестов

**Что тестировать:**
- Полный цикл создания карточки (Create -> Update -> Delete)
- Фильтрация карточек с реальными данными
- Очередь изучения с различными фильтрами
- SRS алгоритм с различными оценками

---

### 10. Логирование важных операций

**Что логировать:**
- Создание/обновление/удаление карточек
- Ошибки при работе с внешними провайдерами словаря
- Медленные запросы (если время выполнения > threshold)
- Ошибки валидации

**Рекомендация:** Использовать структурированное логирование (slog уже используется).

---

### 11. Метрики и мониторинг

**Что отслеживать:**
- Количество созданных карточек
- Количество повторений (reviews)
- Время выполнения запросов
- Ошибки по типам
- Использование внешних провайдеров словаря

---

### 12. Оптимизация запросов

**Потенциальные проблемы:**
- Фильтрация по тегам в `StudyService.GetQueue` выполняется в памяти после получения всех карточек
- Поиск по тексту в `CardService.List` может быть медленным на больших объемах данных

**Улучшения:**
- Добавить индексы в БД (если еще нет)
- Рассмотреть использование полнотекстового поиска (PostgreSQL tsvector)
- Оптимизировать запросы с JOIN

---

## 📊 Приоритизация

### Срочно (для базовой работоспособности)
1. ✅ Dictionary Service: Обработка ErrLexemeNotFound - ВЫПОЛНЕНО
2. ✅ Card Service: Валидация SenseID при создании - ВЫПОЛНЕНО

### Важно (для стабильности)
3. Card Service: Улучшение валидации CustomText
4. Валидация лимитов в фильтрах

### Желательно (для качества)
5. Godoc комментарии
6. Unit-тесты
7. Логирование
8. Метрики

---

## Итоговая оценка готовности

**Текущее состояние:** 🟢 Отличное (90-95%)

**Блокеры для продакшена:**
- ✅ Нет критических блокеров
- ✅ Обработка ошибок и валидация добавлены

**Выполнено:**
1. ✅ Исправлена обработка ErrLexemeNotFound
2. ✅ Добавлена валидация SenseID при создании карточки

**Рекомендации для дальнейшего улучшения:**
1. Добавить unit-тесты для критичных методов (2-4 часа)
2. Настроить логирование и метрики (1-2 часа)
3. Добавить godoc комментарии (30 минут - 1 час)

**Общее время на доработку:** ~3-7 часов для полной готовности к продакшену с тестами и мониторингом.

